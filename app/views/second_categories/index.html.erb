<% content_for :title, 'カテゴリー' %>
<%= link_to '新規追加', new_second_category_path, class: "btn btn-default" %>
<table class="table table-bordered">
  <thead>
  <tr>
    <th>id</th>
    <th>名称</th>
    <th>編集</th>
  </tr>
  </thead> 
  <% @lists.each do |item| %>
    <tbody>
      <tr>
        <th><%=item.id %></th>
        <th><%=item.name %></th>
        <th><%= link_to '編集', edit_second_category_path(item),class: "btn btn-default" %></th>
      </tr>
    </tbody>
  <% end %>
</table>
